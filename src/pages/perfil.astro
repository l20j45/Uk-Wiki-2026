---
import Layout from "../layouts/Layout.astro";
import UserQR from "../components/QRCode.astro"; // <-- Importamos
const { user } = Astro.locals;

// if (!user) return Astro.redirect("/login");
---

<Layout title="Mi Perfil">
  <main class="max-w-md mx-auto p-6">
    <div class="mt-10">
      <h2 class="text-center text-xs font-black uppercase text-slate-400 mb-4">Tu Identificador Digital</h2>
      <UserQR username={"test"} />
    </div>
    
    </main>
</Layout>