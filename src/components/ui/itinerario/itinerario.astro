---
import { getAllItinerary } from "@lib/getAllItinerary";
import { TodayScheduleCard } from "./TodayScheduleCard";

const scheduleArray = await getAllItinerary();

---
<TodayScheduleCard client:only scheduleArray={scheduleArray} />
